# wb-dev (Claude Code)

**When:** PR-readiness — lint, smoke, UT, ST on **tiantiyun** (use `dev.quick`, run `wb-build` first).

```bash
bash scripts/harness/verify_skill.sh --skill wb-dev
python3 scripts/harness/ds_harness.py dev --profile dev.quick
```

Canonical: `.skills/wb-dev/SKILL.md`
