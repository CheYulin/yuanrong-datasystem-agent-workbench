# wb-perf (Claude Code)

**When:** Hotspot/regression diagnosis from perf, bpftrace, strace, bench on **tiantiyun**.

```bash
bash scripts/harness/verify_skill.sh --skill wb-perf
python3 scripts/harness/ds_harness.py perf --profile perf.hotspot --dry-run --json
```

Canonical: `.skills/wb-perf/SKILL.md`
