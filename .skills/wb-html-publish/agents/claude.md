# wb-html-publish (Claude Code)

**When:** Publish HTML to **yche.me** via xqyun `/var/www/html` git.

```bash
bash scripts/harness/verify_skill.sh --skill wb-html-publish
bash scripts/development/sync/publish_htmls_git.sh status
```

Canonical: `.skills/wb-html-publish/SKILL.md`
