# wb-build (Claude Code)

**When:** Compile datasystem, compare CMake/Bazel, or analyze build timing on **tiantiyun**.

```bash
bash scripts/harness/verify_skill.sh --skill wb-build
python3 scripts/harness/ds_harness.py build --backend cmake --profile build.quick
```

Canonical: `.skills/wb-build/SKILL.md`
