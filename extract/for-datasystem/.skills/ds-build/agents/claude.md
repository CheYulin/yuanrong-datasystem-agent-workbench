# ds-build (Claude Code)

**When:** Compile datasystem, compare CMake/Bazel, or analyze build timing on **tiantiyun**.

```bash
bash .skills/ds-harness/scripts/verify_skill.sh --skill ds-build
python3 .skills/ds-harness/scripts/ds_harness.py build --backend cmake --profile build.quick
```

Canonical: `.skills/ds-build/SKILL.md`
