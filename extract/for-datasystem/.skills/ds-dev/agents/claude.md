# ds-dev (Claude Code)

**When:** PR-readiness — lint, smoke, UT, ST on **tiantiyun** (use `dev.quick`, run `ds-build` first).

```bash
bash .skills/ds-harness/scripts/verify_skill.sh --skill ds-dev
python3 .skills/ds-harness/scripts/ds_harness.py dev --profile dev.quick
```

Canonical: `.skills/ds-dev/SKILL.md`
